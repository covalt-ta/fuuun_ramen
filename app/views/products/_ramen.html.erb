<div class="product-list">
  <% ramens.each do |ramen| %>
    <div class="card border-0 product-card" style="width: 18rem;">    
      <%= link_to product_path(ramen), class: "card-link" do %> 
        <div data-size="300x300" class="card-img-top">
          <%= image_tag ramen.image.variant(resize:'300x300'), class: "works-link hover-action h-25" if ramen.image.attached? %>
        </div>
        <div class="card-body">
          <div class="card-top">
            <h5 class="card-title"><%= ramen.name %></h5>
            <p><%= ramen.display_price %></p>
          </div>
          <div class="grad-wrap">
            <div class="grad-item card-text"><%= ramen.text %></div>
          </div>
        </div>
      <% end %>
      <ul class="list-group list-group-flush">
        <li class="list-group-item"><%= link_to "トッピングを選ばないで買い物かごに入れる", product_add_to_baskets_path(ramen) , method: :post, class: "btn btn-outline-dark btn-block" %></li>
      </ul>
    </div>
  <% end %>
</div>