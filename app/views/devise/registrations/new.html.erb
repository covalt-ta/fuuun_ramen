<% breadcrumb :user_registrations %>
<div class="wrapper">
  <div class="inner">
    <h3 class="user-new-headtitle">新規会員登録</h3>
    <div class="user-new-contents">
      <%= form_with model: resource, as: resource_name, url: registration_path(resource_name), class: 'registration-main', local: true do |f| %>
        <%= render 'shared/error_messages', model: f.object %>
        <div class="user-new-form">

          <div class="user-form-group">
            <%= f.label :ニックネーム, class: 'lavel' %>
            <%= f.text_field :nickname, class:"user-input-default", placeholder:"例) ラーメン次郎", maxlength:"40", autofocus: true %>
          </div>

          <div class="user-form-group">
            <%= f.label :メールアドレス, class: 'lavel' %>
            <%= f.email_field :email, class:"user-input-default", placeholder:"PC・携帯どちらでも可" %>
          </div>

          <div class="user-form-group">
            <%= f.label :パスワード, class: 'lavel' %>
            <%= f.password_field :password, class:"user-input-default", placeholder:"6文字以上の半角英数字" %>
          </div>

          <div class="user-form-group">
            <%= f.label :パスワード（確認）, class: 'lavel' %>
            <%= f.password_field :password_confirmation, class:"user-input-default", placeholder:"同じパスワードを入力して下さい" %>
          </div>

          <div class="user-form-group">
            <%= f.label :お名前（全角）, class: 'lavel' %>
            <div class='user-name-wrap'>
              <%= f.text_field :last_name, class:"user-input-name", placeholder:"例) 山田" %>
              <%= f.text_field :first_name, class:"user-input-name", placeholder:"例) 陸太郎" %>
            </div>
          </div>

          <div class="user-form-group">
            <%= f.label :お名前カナ（全角）, class: 'lavel' %>
            <div class='user-name-wrap'>
              <%= f.text_field :last_name_kana, class:"user-input-name", placeholder:"例) ヤマダ" %>
              <%= f.text_field :first_name_kana, class:"user-input-name", placeholder:"例) リクタロウ" %>
            </div>
          </div>

          <div class="user-form-group">
            <%= f.label :生年月日, class: 'lavel' %>
            <div class='user-birth-wrap'>
              <%= raw sprintf(
                          f.date_select(
                            :birthday,
                            class:'select-birth',
                            use_month_numbers: true,
                            prompt:'--',
                            start_year: 1930,
                            end_year: (Time.now.year - 5),
                            date_separator: '%s'),
                          "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" %>
            </div>
          </div>

          <div class="agreement">
            <%= link_to "会員規約", "#", class: "agreement-link" %>
          </div>

          <div class='register-btn'>
            <%= f.submit "規約に同意して会員登録する" ,class:"register-green-btn" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

